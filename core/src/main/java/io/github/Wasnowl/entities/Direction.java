package io.github.Wasnowl.entities;

/**
 * Directions cardinales utilisees par les animateurs.
 */
public enum Direction {
    /** Vers le bas. */
    DOWN,
    /** Vers la gauche. */
    LEFT,
    /** Vers le haut. */
    UP,
    /** Vers la droite. */
    RIGHT
}
